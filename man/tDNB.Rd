% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tDNB.R
\name{tDNB}
\alias{tDNB}
\title{Topological DNB analysis}
\usage{
tDNB(
  expr,
  state,
  state.levels,
  cor.method = "pearson",
  p.adjust.method = "BH",
  variation.method = "sd",
  min.size = 10,
  max.size = 2000,
  AddModuleSize = FALSE,
  power.vec = c(seq_len(10), seq(from = 12, to = 20, by = 2)),
  network.type = "signed",
  RsquaredCut = 0.85
)
}
\arguments{
\item{expr}{A expression dataframe with gene rows and sample columns.}

\item{state}{A time-series dataframe with two columns, the first is the
sample names and the second is the group or time point information.}

\item{state.levels}{A vector for state sequence.}

\item{cor.method}{specifies the method for correlation analysis.}

\item{p.adjust.method}{correction method, a character string. Can be
abbreviated. c("holm", "hochberg", "hommel", "bonferroni", "BH", "BY",
"fdr", "none")}

\item{variation.method}{specifies the method for calculating gene variation.
sd or cv.}

\item{min.size}{Minimum gene number of gene modules.}

\item{max.size}{Maximum gene number of gene modules.}

\item{AddModuleSize}{Whether to consider gene module size when calculating
DNB score.}

\item{power.vec}{a vector of soft thresholding powers for which the scale
free topology fit indices are to be calculated.}

\item{network.type}{network type. Allowed values are (unique abbreviations
of) "unsigned" or "signed".}

\item{RsquaredCut}{desired minimum scale free topology fitting index R^2.}
}
\value{
A list containing:
\describe{
\item{DNB.score}{A data.frame with composite index (CI) values for each
state, including V_in (mean variation), R_in (mean correlation within
DNB), and R_out (mean correlation with non-DNB genes)}
\item{DNB.genes}{Character vector of genes in the identified DNB module}
\item{CI_all}{List of data.frames with CI scores for all candidate modules
in each state}
\item{Gene_module}{List of gene modules detected in each state}
\item{Candidate}{Data.frame summarizing the best candidate module per
state}
\item{RawCor}{List of correlation matrices for each state}
\item{Tom}{List of topological overlap matrices for each state}
\item{V}{List of gene variation values for each state}
\item{SFNet}{List of scale-free network fitting results}
}
}
\description{
Performing topological dynamic network biomarker analysis using scale-free
network topology. This is a novel method developed by Liu Z. that leverages
topological overlap matrix (TOM) for more robust DNB detection.
}
\examples{
# Create example data
set.seed(42)
n_genes <- 100
n_samples <- 15

expr <- matrix(
  rnorm(n_genes * n_samples, mean = 10, sd = 2),
  nrow = n_genes, ncol = n_samples
)
rownames(expr) <- paste0("Gene", seq_len(n_genes))
colnames(expr) <- paste0("Sample", seq_len(n_samples))

state <- data.frame(
  sample_id = colnames(expr),
  state = rep(c("A", "B", "C"), each = 5)
)

\donttest{
result <- tDNB(
  expr = expr,
  state = state,
  state.levels = c("A", "B", "C"),
  min.size = 5,
  max.size = 50
)
result$DNB.score
}
}
\author{
Zaoqu Liu; E-mail: liuzaoqu@163.com
}
